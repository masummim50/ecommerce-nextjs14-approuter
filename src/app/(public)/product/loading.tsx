import DynamicLoading from '@/app/components/shared/DynamicLoading';
import React from 'react';

const loading = () => {
    return (
        <DynamicLoading text="Loading..."/>
    );
};

export default loading;