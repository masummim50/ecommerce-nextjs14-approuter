"use client"
import React from 'react';

const StoreError = () => {
    return (
        <div>
            an error occured while fetching store data
        </div>
    );
};

export default StoreError;